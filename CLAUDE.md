@AGENTS.md

---

## ⚠️ 중요: 커밋 메시지 생성 규칙

**AI가 모든 커밋 메시지를 생성할 때 반드시 준수해야 할 규칙:**

### 1. 언어 규칙 (필수)
- **커밋 메시지는 100% 한국어로 작성**
- 기술 용어만 영어 사용 가능 (API, TypeScript, React 등)
- 절대 영어로 작성하지 말 것

### 2. 형식
```
타입(범위): 설명
```

### 3. 예시 (정확히 따를 것)
✅ 올바른 예시:
- `feat: 마케팅 예제 페이지 추가 (레이아웃, 모달, 테이블, 타이포그래피)`
- `fix: API 응답 타입 오류 수정`
- `feat(auth): 사용자 인증 로직 구현`
- `fix(ui): 버튼 스타일 개선`
- `docs: README 업데이트`

❌ 절대 금지 (영어):
- `feat: add marketing examples`
- `fix: fix button style`

### 4. 타입 목록 (한글 표기 권장)
- `feat` (기능): 새로운 기능 추가
- `fix` (수정): 버그 수정
- `docs` (문서): 문서 변경
- `style` (스타일): 코드 형식 변경
- `refactor` (리팩토링): 코드 재구조화
- `test` (테스트): 테스트 추가/수정
- `chore` (유지보수): 빌드, 의존성 등

### 5. 강제 사항
**이 규칙을 어기면 안 됨. 항상 한국어로 생성할 것.**
